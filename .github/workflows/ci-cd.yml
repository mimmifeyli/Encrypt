name: CI/CD Pipeline

on:
  push:
    branches:
      - main
      - development
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Install .NET SDK 8.0.405
      run: |
        wget https://dotnet.microsoft.com/download/dotnet/thank-you/dotnet-sdk-8.0.405-linux-x64-installer
        sudo dpkg -i dotnet-sdk-8.0.405-linux-x64-installer.deb

    - name: Setup .NET SDK 8.0
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: '8.0.405'

    - name: Restore dependencies
      run: dotnet restore

    - name: Build
      run: dotnet build --configuration Release --no-restore

    - name: Test
      run: dotnet test --no-restore --verbosity normal

  deploy:
    runs-on: ubuntu-latest
    needs: build
    if: github.ref == 'refs/heads/main'

    steps:
    - uses: actions/checkout@v2

    - name: Setup .NET SDK 8.0
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: '8.0.405'

    - name: Publish
      run: dotnet publish -c Release -o ./publish

    - name: Deploy to AWS Elastic Beanstalk
      uses: einaregilsson/beanstalk-deploy@v18
      with:
        aws_access_key: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws_secret_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        application_name: EncryptionAPI2
        environment_name: EncryptionAPI2-env
        version_label: ${{ github.sha }}
        region: us-east-1
        deployment_package: ./publish
